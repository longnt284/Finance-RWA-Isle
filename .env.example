# Default for Vercel or any host that exposes the bundled /api/quotes function.
VITE_EQUITY_FEED_URL=/api/quotes

# For a static frontend hosted separately from the quote proxy, use an absolute URL:
# VITE_EQUITY_FEED_URL=https://market-api.example.com/api/quotes
